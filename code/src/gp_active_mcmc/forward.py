from __future__ import annotations

import numpy as np


def forward_model(theta: np.ndarray) -> np.ndarray:
    """
    Replace this with your simulator (e.g., modelF.coarse_model).
    Must return a 1D array (T,) or (n_outputs,).
    """
    raise NotImplementedError("Connect your coarse_model here.")
